defaults:
  - base
  - _self_

label_ops:
  download_labels:
    enabled: true
    force: true
    output_path: ${paths.data_dir}/labels.jsonl
  create_pairs:
    enabled: true
    force: true
    labels_path: ${label_ops.download_labels.output_path}
    output_dir: ${paths.data_dir}/pairs
