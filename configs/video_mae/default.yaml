backbone: MCG-NJU/videomae-base

training:
  manifest: ${paths.manifests_dir}/train.jsonl
  batch_size: 4
  num_workers: 2
  num_frames: 16
  device: cuda
  output_dir: ${paths.project_root}/runs/video_mae
