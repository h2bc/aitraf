backbone: MCG-NJU/videomae-base
manifests_dir: ${paths.manifests_dir}
experiment_name: aitraf_video_mae
sample_frames: 16
device: cuda
batch_size: 4
num_workers: 4

training:
  run_name: base_train_test
  epochs: 1
  max_train_samples: 10
  output_dir: ${paths.project_root}/runs/video_mae_train

evaluation:
  run_name: base_eval_test
  model_id: null
  output_dir: ${paths.project_root}/runs/video_mae_eval
