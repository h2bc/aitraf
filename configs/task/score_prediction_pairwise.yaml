name: score_prediction_pairwise
pairwise_labels_path: ${paths.data_dir}/pairwise_labels.jsonl
source_manifests_dir: ${paths.manifests_dir}/score_prediction
manifests_dir: ${paths.manifests_dir}/${.name}
vocab_path: ${.manifests_dir}/vocab.json
