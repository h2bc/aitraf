name: trick_classification
manifest_type: pointwise
video_col: video
required_cols: [video, trick]
stratify_col: trick
stratify_strategy: label
manifests_dir: ${paths.manifests_dir}/${.name}
vocab_path: ${.manifests_dir}/vocab.json
