name: score_prediction_rank
manifest_type: pairwise
pairwise_labels_path: ${download_pairwise_labels.output_path}
labels_path: ${download_labels.output_path}
pairwise_labels_required_cols: [left, right, pref]
labels_required_cols: [video, trick]
stratify_col: null
manifests_dir: ${paths.manifests_dir}/${.name}
vocab_path: ${.manifests_dir}/vocab.json
