# aitraf-api

`aitraf-api` is intentionally empty for this refactor.

This package reserves the future inference surface for trick recognition and
trick assessment, but it does not currently contain routes, schemas,
operations, adapters, smoke commands, or service startup code. Future API work
should compose shared runtime helpers from `aitraf-core` and must not depend on
`aitraf-train`.
