name: aitraf
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - --index-url https://download.pytorch.org/whl/cu126
      - --extra-index-url https://pypi.org/simple
      - torch==2.8.0
      - torchvision==0.23.0
      - torchaudio==2.8.0
      - ultralytics
      - opencv-python
      - python-dotenv
      - ipykernel
