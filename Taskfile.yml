version: "3"

includes:
  train:
    taskfile: packages/aitraf-train/Taskfile.yml
    dir: .

tasks:

  lint:
    desc: Run ruff linting
    cmds: 
      - uv run ruff check . {{.CLI_ARGS}}

  format:
    desc: Run ruff formatting 
    cmds: 
      - uv run ruff format . {{.CLI_ARGS}}
